[package]
name = "my_project_name"
version = "0.1.0"
edition = "2021"

[dependencies]
rig-core = { version = "0.6.0", features = ["derive"] }  # This still needs to be rig-core
rig-mongodb = "0.2.1"
mongodb = "3.1.0"
tokio = { version = "1.34.0", features = ["full"] }
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
tracing = "0.1"
dotenv = "0.15"
uuid = { version = "1.0", features = ["v4"] }